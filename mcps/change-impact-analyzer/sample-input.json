{
  "data": {
    "app": "App1",
    "changes": {
      "description": "Updated user authentication system - changed password validation logic, removed middleName field from User model, updated token generation",
      "files": [
        "Models/User.cs",
        "Services/AuthService.cs",
        "Controllers/AuthController.cs",
        "Migrations/20241229_RemoveMiddleName.cs"
      ],
      "diff": "+    public string PasswordHash { get; set; }\n+    // New password validation regex\n+    private static Regex PasswordRegex = new Regex(@\"^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d).{8,}$\");\n-    public string MiddleName { get; set; }\n-    // Old validation\n-    private static bool ValidatePassword(string password) { return password.Length >= 6; }",
      "breaking": true,
      "schemaChange": true
    },
    "context": "Part of security enhancement initiative. Requirements from security audit to strengthen password requirements and remove unused fields to reduce data exposure."
  }
}
